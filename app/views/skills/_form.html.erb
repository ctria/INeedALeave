<%= form_for(@skill) do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :skill_type %><br>
    <%= select_tag(:skill_type, options_for_select([['Language'], ['SBR']], @skill.skill_type)) %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :min_coverage %><br>
    <%= f.number_field :min_coverage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
